# 部署手册（Cloudflare Workers · 约 5 分钟）

> 前置：代码已就绪（本地 `npx wrangler dev` 全链路验证通过）。只差你的 Cloudflare 账号授权。

## 一、登录并建 KV（一次性）

```bash
npx wrangler login                      # 浏览器里点授权
npx wrangler kv namespace create SESSION_KV   # 复制输出里的 id
npx wrangler kv namespace create SAVE_KV      # 复制输出里的 id
```

把两个 id 填进 `wrangler.toml` 的 `[[kv_namespaces]]`（替换 REPLACE_WITH_...）。

## 二、注入密钥（不进代码）

```bash
npx wrangler secret put ZHIHU_OAUTH_APP_KEY   # 粘贴 .dev.vars 里的值
npx wrangler secret put LLM_API_KEY           # 有 LLM Key 时填（OpenAI 兼容），没有可跳过
```

## 三、配置回调并部署

1. 先部署一次拿到域名：`npx wrangler deploy` → 输出形如 `https://chuanshu-engine.<你的子域>.workers.dev`
2. 在 `wrangler.toml` 把 `ZHIHU_OAUTH_REDIRECT_URI` 设为：
   `https://chuanshu-engine.<你的子域>.workers.dev/auth/callback`
3. **同一个地址**登记到知乎黑客松赛事页（OAuth 回调地址栏）
4. 再 `npx wrangler deploy`

## 四、验收清单

- [ ] 打开 `https://…workers.dev/` 首页正常、两本书可玩（AVG 全流程到结局）
- [ ] `/api/health` 返回 `{"ok":true}`
- [ ] 点「知乎登录」→ 知乎授权页 → 回调后首页右上角显示昵称
- [ ] （配了 LLM_KEY）「全部故事」任选一本 → 进度条走完 → 进入生成世界
- [ ] 剧情体验页无「本地预置模式」角标（说明 GM 通道已通）

## 常见问题

- **redirect_uri mismatch**：检查 wrangler.toml 的值与赛事页登记值逐字符一致（协议/域名/路径）
- **KV 报 binding 错误**：id 没填或没重新 deploy
- **forge 报 LLM_NOT_CONFIGURED**：`wrangler secret list` 确认 LLM_API_KEY，且 [vars] 里 LLM_BASE_URL/LLM_MODEL 已填
