{
  "name": "chuanshu-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "check": "node --check server.mjs && node --check worker/index.js && node --check public/js/engine.js && node --check public/js/dsl.js",
    "test": "node --test tests/*.test.mjs",
    "validate": "node tools/validate.mjs public/data/books/btg_room.json"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "hono": "^4.13.7"
  }
}
